package com.Trees.BinaryTree;

import java.util.ArrayList;
import java.util.List;

public class App15_PrintRootToNodePath {

    /*
        public static boolean getPath(List<Integer> al, Node root, int x){

            al.add(root.data);

            if(root.data == x){
                return true;
            }
            if(getPath(al,root.left,x) || getPath(al,root.right,x)){
                return true;
            }
            al.remove(al.size()-1);
            return false;
        }
    */
    public static void main(String[] args) {
        List<Integer> al = new ArrayList<>();
        // getPath(al,root,x)
        // return al;
    }
}
