package com.Trees.BinarySearchTree;

public class App04_FloorInBST {
        /*
            public static int findFloorVal(BinaryTreeNode<Integer>node,int input,int floorVal){
                if(node == null){
                    return floorVal;
                }
                if(node.data <= input){
                    if(node.data > floorVal){
                        floorVal = node.data;
                        return findFloorVal(node.right,input,floorVal);
                    }
                }
                else{
                    return findFloorVal(node.left,input,floorVal);
                }
            }
        */
    public static void main(String[] args) {
        /*
            int floorVal = -1;
		    return findFloorVal(node,input,floorVal);
        */
    }
}
