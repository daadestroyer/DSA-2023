package com.Trees.BinarySearchTree;

public class App05_InsertNodeInBST {
    public static void main(String[] args) {
        /*
            public TreeNode insertIntoBST(TreeNode root, int val) {
                if(root == null){
                    return TreeNode(root);
                }
                TreeNode curr = root;

                while(curr != null){
                    if(curr.val <= val){
                        // insert into right
                        if(curr.right != null){
                            curr = curr.right;
                        }
                        else{
                            curr.right = new TreeNode(val);
                            break;
                        }
                    }
                    else{
                        // insert into left
                        if(curr.left != null){
                            curr = curr.left;
                        }
                        else{
                            curr.left = new TreeNode(val);
                            break;
                        }
                    }
                }
            }
        */
    }
}
