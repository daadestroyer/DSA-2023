package com.Trees;

public class App14_RightViewOfBT {

    /*
        public void rightSideView(Node root,List<Integer> al,int level){
            if(root == null)
                return;

            if(al.size() == level){
                al.add(root.val);
            }
            rightSideView(root.right,al,level+1);
            rightSideView(root.left,al,level+1);
        }
    */
    public static void main(String[] args) {
        // List<Integer> al = new ArrayList<>();
        // rightSideView(root,al,0);
    }
}
