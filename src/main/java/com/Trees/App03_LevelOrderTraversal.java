package com.Trees;

public class App03_LevelOrderTraversal {
    /*
        public List<List<Integer>> levelOrderTraversal(Node node){
            Queue<Integer> q = new LinkedList<>();
            List<Integer> list = new ArrayList<>();

            if(root == null){
                return list;
            }
            q.add(root);
            while(!q.isEmpty()){
                List<Integer> sublist = new ArrayList<>();
                for(int i=0 ; i<q.size() ; i++{
                    if(q.peek().left != null){
                        q.add(q.peek().left);
                    }
                    if(q.peek().right != null){
                        q.add(q.peek().right);
                    }
                    sublist.add(q.remove().val);
                }
                list.add(sublist);
            }

        }
    */
}
