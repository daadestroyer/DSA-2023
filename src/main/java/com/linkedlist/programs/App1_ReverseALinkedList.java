package com.linkedlist.programs;

public class App1_ReverseALinkedList {
    public static void main(String[] args) {
        /*
        question : 1 -> 2 -> 3 -> 4 -> 5
        answer:    5 -> 4 -> 3 -> 2 -> 1

        public ListNode reverseList(Node head) {
            Node newHead = null;
            Node temp = null;
            while(head != null){
                temp = head.next;
                head.next = newHead;
                newHead = head;
                head = next;
            }

        }

        */
    }
}
