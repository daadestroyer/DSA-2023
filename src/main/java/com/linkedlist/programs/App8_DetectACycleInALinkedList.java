package com.linkedlist.programs;

public class App8_DetectACycleInALinkedList {
    public static void main(String[] args) {
        /*
             3 -> 2 -> 0 -> -4 -> 2

             public boolean hasCycle(Node head) {
                Node slow = head;
                Node fast = head;

                while(fast != null && fast.next != null){
                    slow = slow.next;
                    fast = fast.next.next;

                    if(slow == fast){
                        return true
                    }
                }
                return false;
             }
        */
    }
}
