package com.linkedlist.programs;

public class App7_FindIntersectionOfTwoLinkedList {
    public static void main(String[] args) {
        /*
            A:       a1 -> a2
                               -> c1 -> c2 -> c3 -> X
            B: b1 -> b2 -> b3

            public Node getIntersectionNode(Node headA, Node headB) {

                Node left = headA;
                Node right = headB;

                while(left != right){
                    left = left == null ? headB : left.next;
                    right = right == null ? headA : right.next;
                }
            }
        */
    }
}
